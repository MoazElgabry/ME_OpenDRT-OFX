name: Build ME_OpenDRT All Platforms

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  detect-changes:
    name: Detect build-relevant changes
    runs-on: ubuntu-latest
    outputs:
      should_build: ${{ steps.changes.outputs.should_build }}
      changed_files: ${{ steps.changes.outputs.changed_files }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 2

      - name: Decide whether ME_OpenDRT should build
        id: changes
        env:
          BEFORE: ${{ github.event.before }}
          AFTER: ${{ github.sha }}
        run: |
          set -euo pipefail

          if [ -z "${BEFORE}" ] || [ "${BEFORE}" = "0000000000000000000000000000000000000000" ]; then
            changed_files="$(git ls-files)"
          else
            changed_files="$(git diff --name-only "${BEFORE}" "${AFTER}")"
          fi

          printf '%s\n' "${changed_files}" > changed_files.txt

          if grep -Eq '^(ME_OpenDRT/source/(CMakeLists\.txt|cmake/|core/|hosts/ofx/|ofx-sdk/|shared/|src/|tools/cube_viewer_stub/)|ME_OpenDRT/OFX-Workshop/icons/|\.github/workflows/build-me_opendrt-(all-platforms|linux|macos|macos-arm64-safe|windows)\.yml)' changed_files.txt; then
            echo "should_build=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_build=false" >> "$GITHUB_OUTPUT"
          fi

          {
            echo "changed_files<<EOF"
            cat changed_files.txt
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Summarize decision
        run: |
          if [ "${{ steps.changes.outputs.should_build }}" = "true" ]; then
            {
              echo "ME_OpenDRT build will run."
              echo
              echo "Relevant changes were detected in:"
              printf '%s\n' "${{ steps.changes.outputs.changed_files }}"
            } >> "$GITHUB_STEP_SUMMARY"
          else
            {
              echo "ME_OpenDRT build skipped cleanly."
              echo
              echo "No build-relevant changes were detected."
            } >> "$GITHUB_STEP_SUMMARY"
          fi

  windows:
    needs: detect-changes
    if: github.event_name == 'workflow_dispatch' || needs.detect-changes.outputs.should_build == 'true'
    uses: ./.github/workflows/build-me_opendrt-windows.yml
    secrets: inherit

  macos:
    needs: detect-changes
    if: github.event_name == 'workflow_dispatch' || needs.detect-changes.outputs.should_build == 'true'
    uses: ./.github/workflows/build-me_opendrt-macos.yml
    secrets: inherit

  linux:
    needs: detect-changes
    if: github.event_name == 'workflow_dispatch' || needs.detect-changes.outputs.should_build == 'true'
    uses: ./.github/workflows/build-me_opendrt-linux.yml
    secrets: inherit

  draft-release:
    name: Create ME_OpenDRT Draft Release
    needs:
      - detect-changes
      - windows
      - macos
      - linux
    if: github.event_name == 'push' && needs.detect-changes.outputs.should_build == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Download platform artifacts
        uses: actions/download-artifact@v5
        with:
          path: artifacts

      - name: Resolve package version
        id: version
        working-directory: ME_OpenDRT/source
        run: |
          set -euo pipefail

          version="$(grep -Eo 'set\(OPENDRT_DISPLAY_VERSION "[^"]+"\)' CMakeLists.txt | sed -E 's/.*"([^"]+)".*/\1/' | head -n1)"
          if [ -z "${version}" ]; then
            version="$(grep -Eo 'project\(ME_OpenDRT VERSION [0-9A-Za-z.\-]+' CMakeLists.txt | sed -E 's/.*VERSION ([0-9A-Za-z.\-]+)/\1/' | head -n1)"
          fi
          if [ -z "${version}" ]; then
            echo "Could not determine ME_OpenDRT version from ME_OpenDRT/source/CMakeLists.txt"
            exit 1
          fi
          echo "package_version=${version}" >> "$GITHUB_OUTPUT"

      - name: Prepare draft release metadata
        id: draft_meta
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          SHA: ${{ github.sha }}
          REF_NAME: ${{ github.ref_name }}
          PACKAGE_VERSION: ${{ steps.version.outputs.package_version }}
        run: |
          set -euo pipefail

          if latest_json="$(gh api "repos/${REPO}/releases/latest" 2>/dev/null)"; then
            :
          else
            latest_json=""
          fi
          if [ -n "${latest_json}" ]; then
            latest_tag="$(printf '%s' "${latest_json}" | python3 -c "import json,sys; print(json.load(sys.stdin).get('tag_name',''))")"
            latest_name="$(printf '%s' "${latest_json}" | python3 -c "import json,sys; print(json.load(sys.stdin).get('name',''))")"
            printf '%s' "${latest_json}" | python3 -c "import json,sys; print(json.load(sys.stdin).get('body',''))" > latest_release_body.txt
          else
            latest_tag="unreleased"
            latest_name="ME_OpenDRT"
            : > latest_release_body.txt
          fi

          short_sha="${SHA::7}"
          draft_tag="v${PACKAGE_VERSION}"
          draft_title="ME_OpenDRT v${PACKAGE_VERSION} draft (${REF_NAME} ${short_sha})"
          final_tag="${draft_tag}"

          if existing_release_json="$(gh api "repos/${REPO}/releases/tags/${draft_tag}" 2>/dev/null)"; then
            :
          else
            existing_release_json=""
          fi

          draft_exists="false"
          skip_release="false"
          skip_reason=""

          if [ -n "${existing_release_json}" ]; then
            is_draft="$(printf '%s' "${existing_release_json}" | python3 -c "import json,sys; print(str(json.load(sys.stdin).get('draft', False)).lower())")"
            if [ "${is_draft}" = "true" ]; then
              draft_exists="true"
            else
              echo "Release tag ${draft_tag} already exists and is not a draft; skipping draft release."
              skip_release="true"
              skip_reason="Published release already exists for ${draft_tag}."
            fi
          fi

          {
            echo "DRAFT_TAG=${draft_tag}"
            echo "DRAFT_TITLE=${draft_title}"
            echo "LATEST_TAG=${latest_tag}"
            echo "LATEST_NAME=${latest_name}"
            echo "FINAL_TAG=${final_tag}"
            echo "DRAFT_EXISTS=${draft_exists}"
            echo "SKIP_RELEASE=${skip_release}"
            echo "SKIP_REASON=${skip_reason}"
          } >> "$GITHUB_ENV"

          {
            echo "draft_exists=${draft_exists}"
            echo "skip_release=${skip_release}"
          } >> "$GITHUB_OUTPUT"

      - name: Build draft release notes
        if: steps.draft_meta.outputs.skip_release != 'true'
        run: |
          set -euo pipefail

          cat > draft_release_notes.md <<'EOF'
          <!-- Write public release notes anywhere outside the manager blocks. -->

          <!-- Manager highlights: add short manager-facing highlight text between these markers. -->
          <!-- manager-highlights:start -->
          <!-- manager-highlights:end -->

          <!-- manager-available-stable: false -->

          <!-- manager-diagnostics
          {
            "enabled": false,
            "logSourcePath": {
              "windows": [
                "%LOCALAPPDATA%\\ME_OpenDRT",
                "%TEMP%\\ME_OpenDRT_CubeViewer.log"
              ],
              "macos": [
                "~/Library/Logs/ME_OpenDRT.log",
                "~/Library/Logs/ME_OpenDRT_CubeViewer.log"
              ],
              "linux": [
                "~/.cache/ME_OpenDRT",
                "~/.cache/ME_OpenDRT_CubeViewer.log"
              ]
            },
            "environment": {
              "ME_OPENDRT_DEBUG_LOG": "1",
              "ME_OPENDRT_PERF_LOG": "1",
              "ME_OPENDRT_VIEWER_DIAGNOSTICS": "1"
            }
          }
          -->
          EOF

      - name: Skip draft release because version is already published
        if: steps.draft_meta.outputs.skip_release == 'true'
        run: |
          {
            echo "ME_OpenDRT draft release skipped cleanly."
            echo
            echo "${SKIP_REASON}"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Create or update draft release
        if: steps.draft_meta.outputs.skip_release != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mapfile -t release_files < <(find artifacts -type f \( -name "*.zip" -o -name "*.tar.gz" \) | sort)
          if [ "${#release_files[@]}" -eq 0 ]; then
            echo "No packaged ME_OpenDRT artifacts were found."
            exit 1
          fi

          if [ "${DRAFT_EXISTS}" = "true" ]; then
            gh release edit "${DRAFT_TAG}" \
              --title "${DRAFT_TITLE}"
            gh release upload "${DRAFT_TAG}" \
              --clobber \
              "${release_files[@]}"
            {
              echo "ME_OpenDRT draft release updated."
              echo
              echo "- Draft tag: ${DRAFT_TAG}"
            } >> "$GITHUB_STEP_SUMMARY"
          else
            gh release create "${DRAFT_TAG}" \
              --draft \
              --title "${DRAFT_TITLE}" \
              --notes-file draft_release_notes.md \
              "${release_files[@]}"
            {
              echo "ME_OpenDRT draft release created."
              echo
              echo "- Draft tag: ${DRAFT_TAG}"
            } >> "$GITHUB_STEP_SUMMARY"
          fi
